# release-tools

Shared release automation for Go repositories using Goreleaser, Codeberg, Make,
and Just.

Repos should consume this toolkit as a submodule mounted at `tools/release`.

See `docs/usage.md` for the integration contract.
